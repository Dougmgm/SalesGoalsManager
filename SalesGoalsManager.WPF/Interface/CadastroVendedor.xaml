<Window x:Class="SalesGoalsManager.WPF.Interface.CadastroVendedor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SalesGoalsManager.WPF.Interface"
        Title="Cadastrar Vendedor" Height="180" Width="400"
        ResizeMode="NoResize"
        Background="#334355"
        WindowStartupLocation="CenterScreen">

    <Window.InputBindings>
        <KeyBinding Key="Esc" Command="{Binding [Voltar]}" />
        <KeyBinding Key="F2" Command="{Binding [Salvar]}" />
    </Window.InputBindings>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height=".4*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Nome do Vendedor"
                       Foreground="Gold"/>

            <TextBox Height="25"
                     Text="{Binding Vendedor.NomeVendedor, UpdateSourceTrigger=PropertyChanged}"
                     CharacterCasing="Upper"/>
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom">

            <Button Content="Voltar (ESC)"
                    Height="25"
                    Width="90"
                    Command="{Binding [Voltar]}"
                    Background="#334355"
                    Foreground="White"
                    BorderBrush="White"
                    Margin="0,0,10,0"/>

            <Button Content="Salvar (F2)"
                    Height="25"
                    Width="80"
                    Command="{Binding [Salvar]}"
                    Background="Gold"
                    Foreground="Black"/>
        </StackPanel>
    </Grid>
</Window>
