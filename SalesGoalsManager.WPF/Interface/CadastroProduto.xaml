<Window x:Class="SalesGoalsManager.WPF.Interface.CadastroProduto"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SalesGoalsManager.WPF.Interface"
        mc:Ignorable="d"
        Title="Cadastro Produto" Height="200" Width="400"
        ResizeMode="NoResize"
        Background="#334355"
        WindowStartupLocation="CenterScreen">

    <Window.InputBindings>
        <KeyBinding Key="Esc" Command="{Binding [Voltar]}" />
        <KeyBinding Key="F2" Command="{Binding [Salvar]}" />
    </Window.InputBindings>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Nome do Produto"
                       Foreground="Gold"/>

            <TextBox Height="25"
                     Text="{Binding Produto.NomeProduto, UpdateSourceTrigger=PropertyChanged}"
                     CharacterCasing="Upper"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Categoria"
                       Foreground="Gold"/>

            <ComboBox Height="25"
                      ItemsSource="{Binding ListaCategorias}"
                      SelectedItem="{Binding Produto.Categoria}"/>
        </StackPanel>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom">

            <Button Content="Voltar (ESC)"
                    Height="25"
                    Width="90"
                    Command="{Binding [Voltar]}"
                    Background="#334355"
                    Foreground="White"
                    BorderBrush="White"
                    Margin="0,0,10,0"/>

            <Button Content="Salvar (F2)"
                    Height="25"
                    Width="80"
                    Command="{Binding [Salvar]}"
                    Background="Gold"
                    Foreground="Black"/>
        </StackPanel>
    </Grid>
</Window>
